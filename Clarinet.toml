[project]
name = "restore-apparatus"
authors = ["Marine Data Research Team"]
description = "A decentralized platform for marine ecosystem restoration data tracking and collaboration"
telemetry = false
requirements = []
[contracts.ecosystem-vault]
path = "contracts/ecosystem-vault.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
